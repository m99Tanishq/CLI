name: Development Build

on:
  push:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v4
        with:
          go-version: '1.21'

      - name: Cache Go modules
        uses: actions/cache@v4
        with:
          path: ~/go/pkg/mod
          key: ${{ runner.os }}-go-${{ hashFiles('**/go.sum') }}
          restore-keys: |
            ${{ runner.os }}-go-

      - name: Install dependencies
        run: go mod download

      - name: Run tests
        run: go test -v ./...

      - name: Build for all platforms
        run: |
          GOOS=linux GOARCH=amd64 go build -ldflags="-s -w" -o CLI-linux-amd64 .
          GOOS=linux GOARCH=arm64 go build -ldflags="-s -w" -o CLI-linux-arm64 .
          GOOS=darwin GOARCH=amd64 go build -ldflags="-s -w" -o CLI-darwin-amd64 .
          GOOS=darwin GOARCH=arm64 go build -ldflags="-s -w" -o CLI-darwin-arm64 .
          GOOS=windows GOARCH=amd64 go build -ldflags="-s -w" -o CLI-windows-amd64.exe .
          GOOS=windows GOARCH=arm64 go build -ldflags="-s -w" -o CLI-windows-arm64.exe .

      - name: Generate checksums
        run: |
          sha256sum CLI-linux-amd64 > CLI-linux-amd64.sha256
          sha256sum CLI-linux-arm64 > CLI-linux-arm64.sha256
          sha256sum CLI-darwin-amd64 > CLI-darwin-amd64.sha256
          sha256sum CLI-darwin-arm64 > CLI-darwin-arm64.sha256
          sha256sum CLI-windows-amd64.exe > CLI-windows-amd64.exe.sha256
          sha256sum CLI-windows-arm64.exe > CLI-windows-arm64.exe.sha256

      - name: Upload build artifacts
        uses: actions/upload-artifact@v4
        with:
          name: CLI-builds
          path: |
            CLI-linux-amd64
            CLI-linux-amd64.sha256
            CLI-linux-arm64
            CLI-linux-arm64.sha256
            CLI-darwin-amd64
            CLI-darwin-amd64.sha256
            CLI-darwin-arm64
            CLI-darwin-arm64.sha256
            CLI-windows-amd64.exe
            CLI-windows-amd64.exe.sha256
            CLI-windows-arm64.exe
            CLI-windows-arm64.exe.sha256
          retention-days: 30 